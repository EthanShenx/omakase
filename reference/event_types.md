# Alternative splicing event types

The five event classes rMATS reports.

## Usage

``` r
event_types()
```

## Value

A character vector: `SE`, `A5SS`, `A3SS`, `MXE`, `RI`.

## Examples

``` r
event_types()
#> [1] "SE"   "A5SS" "A3SS" "MXE"  "RI"  
```
