# Locus identifiers held by a sashimi data object

Locus identifiers held by a sashimi data object

## Usage

``` r
loci(x)
```

## Arguments

- x:

  A `sashimi_data` object.

## Value

A character vector of `locus_id` values.

## Examples

``` r
loci(sashimi_data())
#> character(0)
```
