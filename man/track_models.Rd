% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-tracks.R
\name{track_models}
\alias{track_models}
\title{Build a transcript model track}
\usage{
track_models(
  models,
  title = NULL,
  color = "#1F78B4",
  style = c("UCSC", "flat"),
  labels = FALSE,
  chevrons = 18,
  height = 1,
  row_by = c("tx_id", "role")
)
}
\arguments{
\item{models}{A data frame of exons with \code{start}, \code{end} and \code{tx_id}, such as
\code{\link[=read_bed12]{read_bed12()}} or \code{\link[=read_annotation]{read_annotation()}} returns; a \code{sashimi_data} object,
whose \code{models} slot is used; or a path to a BED/GTF/GFF3 file.}

\item{title}{Track title, printed to the right of the panel.}

\item{color}{Fill for the blocks. \code{"bed_rgb"} uses each record's own colour
when the source carried one; a named vector keyed by \code{role} colours by
role.}

\item{style}{\code{"UCSC"} draws CDS taller than UTR; \code{"flat"} draws every block
the same height.}

\item{labels}{Print each transcript's name beside its row.}

\item{chevrons}{Number of direction marks per intron; \code{0} for none.}

\item{height}{Relative height of this track.}

\item{row_by}{Which column separates rows: \code{"tx_id"} (one row per
transcript, the default) or \code{"role"} (one row per role).}
}
\value{
A \code{omakase_track} object, for \code{\link[=plot_tracks]{plot_tracks()}}.
}
\description{
Draws transcript models the way a genome browser does: exons as blocks,
introns as a line, and the direction of transcription marked by chevrons
along that line. With \code{style = "UCSC"} the coding part of each exon is drawn
taller than the untranslated part.
}
\examples{
m <- data.frame(tx_id = "tx1", start = c(1000, 3000), end = c(1500, 4000),
                strand = "+", feature = "CDS")
track_models(m, title = "models")

}
