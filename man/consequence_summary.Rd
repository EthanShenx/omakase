% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consequence-classify.R
\name{consequence_summary}
\alias{consequence_summary}
\title{Summarise a classified switch table}
\usage{
consequence_summary(
  x,
  by = c("subtype", "category"),
  filter = NULL,
  drop = c("Unclassified", "Distal (excluded)")
)
}
\arguments{
\item{x}{A data frame from \code{\link[=classify_consequence]{classify_consequence()}}, or any table with
\code{category} and \code{subtype}.}

\item{by}{\code{"subtype"} (the default) or \code{"category"}.}

\item{filter}{An optional expression evaluated in \code{x} to subset rows before
counting - for example \code{both_full_length == 1}.}

\item{drop}{Categories to exclude. Defaults to the two non-biological ones.}
}
\value{
A data frame with \code{category}, \code{subtype} (when \code{by = "subtype"}),
\code{n}, \code{proportion} and \code{label}.
}
\description{
Counts and proportions per category and subtype, in the order the donut
draws them.
}
\examples{
d <- data.frame(category = c("5'UTR change", "5'UTR change", "Promoter swap"),
                subtype = c("longer", "shorter", "alt first exon"))
consequence_summary(d)

}
