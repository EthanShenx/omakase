% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arcs.R
\name{arc_path}
\alias{arc_path}
\title{Build the path of a junction arc}
\usage{
arc_path(x0, x1, h, shape = "sine", n = 120, y0 = 0)
}
\arguments{
\item{x0, x1}{Genomic start and end of the junction.}

\item{h}{Apex height, in the units of the coverage axis.}

\item{shape}{One of \code{\link[=arc_shapes]{arc_shapes()}}.}

\item{n}{Number of points in the returned path. Higher is smoother; 120 is
plenty at print size, and \code{elbow} ignores it.}

\item{y0}{Baseline the arc springs from. Non-zero when arcs are stacked
above a coverage track that does not start at zero.}
}
\value{
A data frame with columns \code{x} and \code{y}.
}
\description{
Returns the polyline for a single arc running from \code{x0} to \code{x1} and peaking
at height \code{h}. All shapes start and end at \code{y = 0} and reach exactly \code{h} at
their apex, so arcs of different shapes are directly comparable.
}
\details{
The shapes, parameterised by \eqn{t \in [0, 1]} with
\eqn{x(t) = x_0 + (x_1 - x_0)t}:

\describe{
\item{\code{sine}}{\eqn{y = h \sin(\pi t)}. The default: symmetric, with a
gentle take-off from the baseline.}
\item{\code{parabola}}{\eqn{y = 4 h\, t (1 - t)}. Slightly fuller shoulders
than the sine.}
\item{\code{bezier}}{Cubic Bezier
\eqn{B(t) = (1-t)^3 P_0 + 3t(1-t)^2 P_1 + 3t^2(1-t) P_2 + t^3 P_3}
with control points lifted to \eqn{4h/3} so the apex lands on \eqn{h};
this is the curve MISO's \code{sashimi_plot} draws.}
\item{\code{xspline}}{An approximation of the X-spline used by \code{ggsashimi},
which rises steeply from the donor and flattens across the middle.}
\item{\code{elbow}}{Straight risers joined by a flat top - useful when many
arcs overlap and curvature becomes noise.}
\item{\code{arch}}{A semi-ellipse, \eqn{y = h\sqrt{1 - (2t - 1)^2}}, the
roundest of the set.}
}
}
\examples{
head(arc_path(100, 200, h = 10))
# every shape peaks at h
vapply(arc_shapes(), function(s) max(arc_path(0, 1, 5, shape = s)$y), numeric(1))

}
