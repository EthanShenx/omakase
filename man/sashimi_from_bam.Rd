% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-bam.R
\name{sashimi_from_bam}
\alias{sashimi_from_bam}
\title{Read coverage and junctions from alignment files}
\usage{
sashimi_from_bam(
  bam,
  region,
  annotation = NULL,
  bin = NULL,
  n_bins = 800,
  group_col = NULL,
  label_col = NULL,
  strand = "none",
  keep_strand = "both",
  min_count = 1,
  min_mapq = 0,
  flags = NULL,
  per_sample = TRUE,
  gene = NULL,
  flank = 0,
  junction_overlap = c("within", "any")
)
}
\arguments{
\item{bam}{A path to a BAM/CRAM/SAM file, a vector of paths, or a manifest
read by \code{\link[=read_manifest]{read_manifest()}} (a path to a TSV works directly). A \code{.sam} file
is converted to a sorted, indexed BAM in the session's temporary directory
the first time it is read, which is what \code{rmats2sashimiplot} does for its
\code{--s1}/\code{--s2} inputs.}

\item{region}{A region string such as \code{"chr10:27035000-27050000"}, a
\code{\link[=parse_region]{parse_region()}} object, or a data frame of regions with columns \code{chrom},
\code{start}, \code{end} and optionally \code{name}.}

\item{annotation}{Optional GTF/GFF3 path, or the result of
\code{\link[=read_annotation]{read_annotation()}}, used to draw transcript models under the tracks.}

\item{bin}{Bin width in base pairs. \code{1} gives per-base coverage, which is
exact but slow to draw over a wide window; the default of \code{NULL} picks a
bin that gives roughly \code{n_bins} points.}

\item{n_bins}{Target number of bins when \code{bin} is \code{NULL}.}

\item{group_col, label_col}{Manifest columns used for grouping and panel
labels; see \code{\link[=read_manifest]{read_manifest()}}.}

\item{strand}{Strand specificity, see \code{\link[=strand_modes]{strand_modes()}}.}

\item{keep_strand}{Which strand to keep once reads are assigned: \code{"both"},
\code{"+"} or \code{"-"}. Only meaningful when \code{strand} is not \code{"none"}.}

\item{min_count}{Drop junctions with fewer supporting reads.}

\item{min_mapq}{Ignore alignments below this mapping quality.}

\item{flags}{A \code{\link[Rsamtools:ScanBamParam-class]{Rsamtools::scanBamFlag()}} value. The default drops
unmapped reads, secondary alignments, duplicates and QC failures.}

\item{per_sample}{Keep one track per sample. When \code{FALSE}, samples sharing a
group are summed as they are read, which is cheaper in memory.}

\item{gene}{Restrict \code{annotation} to this gene name.}

\item{flank}{Widen the region by this many base pairs on each side.}

\item{junction_overlap}{Which junctions to keep: \code{"within"} (the default)
keeps only junctions whose donor and acceptor both fall inside the region,
\code{"any"} also keeps those with one end beyond it, whose arcs then run off
the edge of the panel.}
}
\value{
A \code{sashimi_data} object.
}
\description{
Reads one or more BAM/CRAM files over a region and returns a
\code{\link[=sashimi_data]{sashimi_data()}} object holding binned coverage and spliced-junction counts.
This is the main entry point when starting from alignments.
}
\details{
Coverage is the depth of reference-consuming CIGAR operations (\code{M}, \code{D},
\code{=}, \code{X}), binned to \code{bin} base pairs by taking the mean depth within each
bin. Junctions are \code{N} operations, counted once per read and keyed by their
donor and acceptor coordinates; a junction supported by fewer than
\code{min_count} reads is dropped.

For a stranded library, set \code{strand} so that reads are assigned to the
correct strand, and \code{keep_strand} to draw only one of them. For an
unstranded library leave \code{strand = "none"} and everything is pooled.
}
\examples{
bams <- system.file("extdata", "samples.tsv", package = "omakase")
gtf <- system.file("extdata", "annotation.gtf", package = "omakase")
if (nzchar(bams)) {
  sd <- sashimi_from_bam(bams, "chr10:27040584-27048100", annotation = gtf,
                         min_count = 10)
  plot_sashimi(sd, aggregate = "mean")
}

}
