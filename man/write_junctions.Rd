% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-bed12.R
\name{write_junctions}
\alias{write_junctions}
\title{Write junctions to a BED file}
\usage{
write_junctions(x, file, anchor = 25, name_prefix = "JUNC")
}
\arguments{
\item{x}{A \code{sashimi_data} object, or a junction data frame with \code{x0}, \code{x1}
and \code{count}.}

\item{file}{Output path.}

\item{anchor}{Width of the flanking blocks, in base pairs.}

\item{name_prefix}{Prefix for the junction names.}
}
\value{
\code{file}, invisibly.
}
\description{
Writes the \code{junctions} slot as a BED12 file in the layout regtools and
TopHat use: two anchor blocks either side of the intron, the score column
carrying the supporting read count. This is the counterpart of \code{ggsashimi}'s
\code{--junctions-bed}, and lets counted junctions be loaded into a genome
browser or fed back in through \code{\link[=read_junctions]{read_junctions()}}.
}
\examples{
j <- data.frame(locus_id = "a", group = "g", x0 = 1000, x1 = 2000,
                count = 42, chrom = "chr1")
f <- tempfile(fileext = ".bed")
write_junctions(j, f)
readLines(f)
}
