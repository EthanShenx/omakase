% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-bed12.R
\name{read_bed}
\alias{read_bed}
\alias{read_bed12}
\title{Read a BED file}
\usage{
read_bed(path, region = NULL, expand = TRUE, name_col = 4)

read_bed12(path, region = NULL, expand = TRUE, name_col = 4)
}
\arguments{
\item{path}{Path to the BED file, optionally gzipped.}

\item{region}{Optional region to restrict to.}

\item{expand}{For BED12, expand blocks into one row per exon (the default).
\code{FALSE} keeps one row per transcript.}

\item{name_col}{Column holding the transcript name; defaults to column 4.}
}
\value{
A data frame with \code{chrom}, \code{start}, \code{end}, \code{tx_id}, \code{strand},
\code{feature}, and \code{color} when the file carries an \code{itemRgb} column.
Coordinates are 1-based inclusive.
}
\description{
Reads BED3 through BED12. For BED12 the blocks are expanded into exons and
each exon is split against the thick range, so the result carries a
\code{feature} column of \code{"CDS"} and \code{"UTR"} that a genome track can draw at
different heights.
}
\examples{
f <- tempfile()
writeLines(paste("chr1", 1000, 5000, "tx1", 0, "+", 1200, 4000,
                 "31,120,180", 2, "500,800,", "0,3200,", sep = "\t"), f)
read_bed(f)

}
