% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-annotation.R
\name{read_annotation}
\alias{read_annotation}
\title{Read a GTF or GFF3 annotation}
\usage{
read_annotation(
  path,
  region = NULL,
  feature = c("exon", "CDS"),
  gene = NULL,
  transcript = NULL
)
}
\arguments{
\item{path}{Path to a \code{.gtf}, \code{.gff}, \code{.gff3} file, optionally gzipped.}

\item{region}{Optional region to restrict the import to. Supplying one is
much faster than reading a whole-genome annotation, and requires a
tabix-indexed file for the fastest path.}

\item{feature}{Which record types to keep. \code{"exon"} is enough to draw a
transcript; adding \code{"CDS"} lets the panel draw coding regions taller than
untranslated ones.}

\item{gene}{Restrict to one or more gene names.}

\item{transcript}{Restrict to one or more transcript identifiers.}
}
\value{
A data frame with columns \code{tx_id}, \code{gene_id}, \code{gene_name}, \code{chrom},
\code{start}, \code{end}, \code{strand} and \code{feature}.
}
\description{
Imports exon and CDS records and returns the tidy transcript models the
annotation panel draws. Format is detected from the file extension, and
gzip-compressed files are handled transparently.
}
\examples{
gtf <- system.file("extdata", "annotation.gtf", package = "omakase")
if (nzchar(gtf)) head(read_annotation(gtf))

}
