% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-contract.R
\name{sashimi_data}
\alias{sashimi_data}
\title{Construct a sashimi data object}
\usage{
sashimi_data(
  loci = NULL,
  tracks = NULL,
  junctions = NULL,
  models = NULL,
  psi = NULL,
  features = NULL,
  meta = list()
)
}
\arguments{
\item{loci, tracks, junctions, models, psi, features}{Data frames as described
above. Any may be \code{NULL}.}

\item{meta}{A named list of provenance/parameter values.}
}
\value{
An object of class \code{sashimi_data}.
}
\description{
The container every \verb{sashimi_from_*()} reader returns and every plotting
function accepts. It is a plain list of tidy data frames, so it can be built
by hand when counts come from a source omakase does not read directly.
}
\section{Slots}{


\describe{
\item{\code{loci}}{One row per plotted locus. Required: \code{locus_id},
\code{gene_name}, \code{chrom}, \code{strand}, \code{win_lo}, \code{win_hi}. Optional: \code{anchor}
(a shared downstream point arcs may point at), \code{main_apex}, \code{alt_apex}
(start sites to mark with a triangle).}
\item{\code{tracks}}{Binned coverage. Required: \code{locus_id}, \code{group}, \code{pos},
\code{value}. Optional: \code{sample}, \code{strand}, \code{bin}.}
\item{\code{junctions}}{Arcs. Required: \code{locus_id}, \code{group}, \code{x0}, \code{x1},
\code{count}. Optional: \code{role}, \code{label}, \code{sample}, \code{strand}. \code{x0}/\code{x1} are
genomic coordinates; \code{count} drives the arc label, and optionally its
width and height.}
\item{\code{models}}{Transcript models drawn beneath the tracks. Required:
\code{locus_id}, \code{tx_id}, \code{start}, \code{end}. Optional: \code{role} (used to colour
and to order the rows), \code{feature} (\code{"exon"}, \code{"CDS"}, \code{"UTR"}),
\code{strand}.}
\item{\code{psi}}{Per-group inclusion values printed in the right-hand gutter.
Required: \code{locus_id}, \code{group}, \code{psi}. Optional: \code{numerator},
\code{denominator}.}
\item{\code{features}}{An extra annotation row under each model - repeats,
motifs, peaks, anything. Required: \code{locus_id}, \code{start}, \code{end}, \code{name}.
Optional: \code{role}, \code{class}, \code{color}.}
\item{\code{meta}}{A named list recording how the object was built. Printed by
\code{summary()} and written to the methods table by \code{\link[=write_sashimi_data]{write_sashimi_data()}}.}
}
}

\examples{
loci <- data.frame(
  locus_id = "demo", gene_name = "DEMO", chrom = "chr1",
  strand = "+", win_lo = 1000, win_hi = 2000
)
tracks <- data.frame(
  locus_id = "demo", group = "A",
  pos = seq(1000, 2000, by = 10),
  value = abs(sin(seq(0, pi, length.out = 101))) * 10
)
sashimi_data(loci = loci, tracks = tracks)

}
