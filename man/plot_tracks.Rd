% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-tracks.R
\name{plot_tracks}
\alias{plot_tracks}
\title{Draw a genome-track figure}
\usage{
plot_tracks(
  x = NULL,
  tracks = NULL,
  region = NULL,
  locus = NULL,
  axis = c("bottom", "top", "none"),
  palette = NULL,
  title = NULL,
  title_width = 0.24,
  left_pad = 0.05,
  base_size = 9,
  base_family = "",
  hairline = 0.3,
  shrink = FALSE,
  shrink_method = "power",
  shrink_gamma = 0.7,
  chrom_style = "keep",
  theme = NULL
)
}
\arguments{
\item{x}{A \code{sashimi_data} object, or \code{NULL} when \code{tracks} is given.}

\item{tracks}{A list of tracks from \code{\link[=track_models]{track_models()}}, \code{\link[=track_coverage]{track_coverage()}},
\code{\link[=track_features]{track_features()}}, \code{\link[=track_axis]{track_axis()}} and \code{\link[=track_spacer]{track_spacer()}}. Built from \code{x}
when \code{NULL}.}

\item{region}{The coordinate range, as a region string or a
\code{\link[=parse_region]{parse_region()}} object. Taken from \code{x} when \code{NULL}.}

\item{locus}{When \code{x} holds several loci, which one to draw.}

\item{axis}{\code{"bottom"}, \code{"top"} or \code{"none"}; ignored when \code{tracks} already
contains an axis.}

\item{palette}{Palette for the per-group coverage tracks.}

\item{title}{Figure title.}

\item{title_width}{Fraction of the figure width reserved for track titles.}

\item{left_pad}{Fraction of the width reserved at the left for the coverage
range labels.}

\item{base_size}{Base font size in points.}

\item{base_family}{Font family for every piece of text in the figure. The
empty string uses the graphics device's default. Text geoms do not inherit
a theme's family, so this is threaded to each of them explicitly.}

\item{hairline}{Line width for rules.}

\item{shrink}{Compress introns; \code{TRUE}, or an \code{\link[=intron_map]{intron_map()}}.}

\item{shrink_method, shrink_gamma}{Compression rule and its exponent.}

\item{chrom_style}{How the contig name is printed: \code{"keep"}, \code{"ucsc"} or
\code{"ensembl"}.}

\item{theme}{A ggplot2 theme, or \code{NULL} for \code{\link[=theme_omakase]{theme_omakase()}}.}
}
\value{
A \code{patchwork} object.
}
\description{
Stacks tracks over a shared coordinate range, with titles down the right and
an axis at top or bottom. This is the browser-style view of a locus, as
opposed to the splicing-focused view \code{\link[=plot_sashimi]{plot_sashimi()}} gives.
}
\details{
Called on a \code{sashimi_data} object with no \code{tracks} argument, it builds a
sensible default stack: one model track, one coverage track per group, a
feature track if the object has features, and an axis. Pass \code{tracks} to take
full control.
}
\examples{
sd <- sashimi_data(
  loci = data.frame(locus_id = "a", gene_name = "DEMO", chrom = "chr1",
                    strand = "+", win_lo = 1000, win_hi = 5000),
  tracks = data.frame(locus_id = "a", group = "sample1",
                      pos = seq(1000, 5000, 40),
                      value = abs(sin(seq(0, pi, length.out = 101)))),
  models = data.frame(locus_id = "a", tx_id = "tx1", role = "main",
                      feature = "CDS",
                      start = c(1200, 3000), end = c(1600, 4200))
)
plot_tracks(sd)

}
