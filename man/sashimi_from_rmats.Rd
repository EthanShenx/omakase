% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-rmats.R
\name{sashimi_from_rmats}
\alias{sashimi_from_rmats}
\title{Build a sashimi data object from rMATS events and alignments}
\usage{
sashimi_from_rmats(
  events,
  bam,
  event_type = NULL,
  fdr = NULL,
  min_dpsi = NULL,
  top = NULL,
  genes = NULL,
  chrom_style = "keep",
  flank = 300,
  group_col = NULL,
  label_col = NULL,
  psi_from = c("rmats", "junctions", "none"),
  ...
)
}
\arguments{
\item{events}{Path to an rMATS output file, or a data frame from
\code{\link[=read_rmats]{read_rmats()}}.}

\item{bam}{A manifest or vector of BAM paths, as for \code{\link[=sashimi_from_bam]{sashimi_from_bam()}}.}

\item{event_type}{One of \code{\link[=event_types]{event_types()}}; inferred when \code{NULL}.}

\item{fdr}{Keep only events with \code{FDR} at or below this.}

\item{min_dpsi}{Keep only events whose absolute \code{IncLevelDifference} is at
least this.}

\item{top}{Keep only the \code{top} most significant events.}

\item{genes}{Restrict to these gene names or IDs.}

\item{chrom_style}{Rewrite contig names: \code{"keep"}, \code{"ucsc"} (ensure a \code{chr}
prefix) or \code{"ensembl"} (strip it), for when the event file and the BAM
header disagree.}

\item{flank}{Padding added around the event when computing its window.}

\item{group_col, label_col}{Manifest columns for grouping and labels.}

\item{psi_from}{\code{"rmats"} takes the inclusion levels rMATS reported;
\code{"junctions"} recomputes PSI from the junctions read out of the BAMs;
\code{"none"} leaves the slot empty.}

\item{...}{Passed to \code{\link[=sashimi_from_bam]{sashimi_from_bam()}} (\code{bin}, \code{strand}, \code{min_count},
\code{min_mapq}, and so on).}
}
\value{
A \code{sashimi_data} object with one locus per event.
}
\description{
Reads an rMATS event file, turns each event into a plotting window with the
event's exons as transcript models, and reads coverage and junctions from
the supplied BAM files over those windows. The result is one locus per
event, ready for \code{\link[=plot_sashimi_all]{plot_sashimi_all()}}.
}
\details{
The two isoforms of an event are drawn as two model rows: the inclusion form
(role \code{"main"}) carries the alternative exon, the skipping form (role
\code{"alt"}) does not. That is the same reading as \code{rmats2sashimiplot}'s
two-row layout, but as data rather than as pixels, so the rows can be
recoloured or relabelled afterwards.
}
\examples{
ev <- system.file("extdata", "SE.MATS.JC.txt", package = "omakase")
bams <- system.file("extdata", "samples.tsv", package = "omakase")
if (nzchar(ev) && nzchar(bams)) {
  sd <- sashimi_from_rmats(ev, bams, min_count = 5)
  plot_sashimi(sd)
}

}
