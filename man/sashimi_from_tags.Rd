% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-tags.R
\name{sashimi_from_tags}
\alias{sashimi_from_tags}
\title{Build a sashimi data object from 5'-tag data}
\usage{
sashimi_from_tags(
  tags,
  region,
  annotation = NULL,
  bin = 25,
  footprint = 250,
  normalize = c("tpm", "none"),
  library_sizes = NULL,
  aggregate = c("mean", "median", "sum", "none"),
  strand = "gene",
  activity = NULL,
  group_col = NULL,
  label_col = NULL,
  gene = NULL
)
}
\arguments{
\item{tags}{A manifest read by \code{\link[=read_manifest]{read_manifest()}}, a path to one, a vector of
BED paths, or a data frame of tags with \code{chrom}, \code{pos}, \code{count},
\code{strand}.}

\item{region}{A region string, or a data frame of regions with \code{chrom},
\code{start}, \code{end} and optionally \code{name} and \code{strand}.}

\item{annotation}{Optional GTF/GFF3 for transcript models.}

\item{bin}{Bin width in base pairs.}

\item{footprint}{Distance each tag is extended toward the 3' end.}

\item{normalize}{\code{"tpm"} scales each library to tags per million; \code{"none"}
leaves raw counts.}

\item{library_sizes}{Named vector of total tag counts per sample. Computed
from the file when absent, which counts only tags inside the window unless
the file is region-restricted already.}

\item{aggregate}{How to combine the samples of a group: \code{"mean"},
\code{"median"}, \code{"sum"}, or \code{"none"} to keep one track per sample.}

\item{strand}{Keep only tags on this strand. \code{"gene"} uses each region's own
strand, which is usually what you want.}

\item{activity}{A data frame of start-site activities to draw as arcs, with
columns \code{locus_id} (or \code{name}), \code{group}, \code{x0} (the start site), \code{x1} (the
anchor), \code{count}, and optionally \code{role}.}

\item{group_col, label_col}{Manifest columns for grouping and labels.}

\item{gene}{Restrict the annotation to this gene.}
}
\value{
A \code{sashimi_data} object.
}
\description{
Turns single-base 5'-end tags (CAGE, STRT, CamoTSS) into the binned tracks a
sashimi panel draws, and optionally into arcs that run from each start site
to a shared downstream anchor.
}
\details{
For each sample the tags on the requested strand inside the window are
binned at \code{bin} base pairs and scaled to tags per million,
\eqn{t_i \times 10^6 / N_j}, using that library's total tag count. Each tag
is then extended \code{footprint} base pairs toward the 3' end, which gives the
track the body of a coverage profile without moving the start site. Finally
the samples of a group are averaged.

Arcs in this mode are not junctions - a 5'-tag protocol produces no spliced
reads to count. An arc here is a pointer from a start site to the body of
the transcript it starts, and its label is that site's activity. Supply
those numbers through \code{activity}; the resulting figure is the one the
\code{omakase} sashimi style was designed for.
}
\examples{
tags <- data.frame(chrom = "chr1", pos = c(1200, 1200, 1800),
                   count = 1, strand = "+", sample = "s1", group = "early")
sashimi_from_tags(tags, "chr1:1000-2000", bin = 25, footprint = 100)

}
