% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arcs.R
\name{arc_widths}
\alias{arc_widths}
\title{Compute arc line widths from junction counts}
\usage{
arc_widths(
  count,
  rule = c("log", "miso", "linear", "constant"),
  w0 = 0.5,
  base = 10,
  alpha = 1,
  range = c(0.15, 2)
)
}
\arguments{
\item{count}{Numeric vector of junction counts.}

\item{rule}{\code{"log"} (the default), \code{"miso"}, \code{"linear"}, or \code{"constant"}.}

\item{w0}{Scale factor \eqn{w_0}.}

\item{base}{Logarithm base \eqn{b}.}

\item{alpha}{Exponent \eqn{\alpha} applied to the log term.}

\item{range}{Numeric length-2 clamp applied to the result.}
}
\value{
A numeric vector of line widths.
}
\description{
The classic sashimi convention is that a junction supported by more reads is
drawn with a thicker curve. The default here is
\deqn{w = w_0 \left(\log_b (n + 1)\right)^{\alpha}}
clamped to \code{range}. Passing \code{rule = "miso"} instead reproduces MISO's
\eqn{w = \log(b)\,(n+1)^{0.33} \times 0.1}, and \code{rule = "constant"} disables
the effect entirely.
}
\examples{
arc_widths(c(1, 5, 50, 500))
arc_widths(c(1, 5, 50, 500), rule = "constant", w0 = 0.5)

}
