% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consequence-orf.R
\name{find_orf}
\alias{find_orf}
\title{Call the open reading frame of an mRNA}
\usage{
find_orf(mrna, rule = c("first", "longest"), min_aa = 10, require_stop = TRUE)
}
\arguments{
\item{mrna}{A \code{DNAString}, \code{DNAStringSet} of length 1, or a character
string.}

\item{rule}{\code{"first"} or \code{"longest"}.}

\item{min_aa}{Ignore reading frames shorter than this many amino acids.}

\item{require_stop}{Require an in-frame stop codon. When \code{FALSE}, a frame
running off the 3' end still counts, which matters for transcripts whose
3' end was not sequenced.}
}
\value{
A list with \code{protein} (character, \code{""} when no ORF was found),
\code{start}, \code{end}, \code{utr5_len}, \code{aa_len} and \code{n_uATG}.
}
\description{
Finds the ORF and returns the peptide it encodes together with the length of
the 5' untranslated region and the number of upstream AUGs.
}
\details{
Two conventions are available. \code{"first"} takes the first AUG in the
sequence that opens a reading frame terminated by a stop codon, which is the
scanning model and the one used to classify start-site consequences.
\code{"longest"} takes the AUG that yields the longest peptide, which is more
robust when the 5' end of the transcript is not trustworthy.
}
\examples{
find_orf("AAAATGGGGCCCTAA")

}
