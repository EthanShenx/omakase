% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consequence-orf.R
\name{splice_mrna}
\alias{splice_mrna}
\title{Splice exons into an mRNA sequence}
\usage{
splice_mrna(exons, strand, genome, chrom)
}
\arguments{
\item{exons}{A data frame with \code{start} and \code{end}, one row per exon.}

\item{strand}{\code{"+"} or \code{"-"}.}

\item{genome}{A \code{BSgenome} object, a \code{DNAStringSet} of chromosomes, or a
path to an indexed FASTA.}

\item{chrom}{The contig the exons lie on.}
}
\value{
A \code{DNAString}.
}
\description{
Concatenates exon sequences in transcription order and reverse-complements
them for a minus-strand transcript, giving the mature mRNA an ORF should be
called on.
}
\examples{
if (requireNamespace("Biostrings", quietly = TRUE)) {
  gen <- Biostrings::DNAStringSet(c(chr1 = strrep("ATGC", 100)))
  splice_mrna(data.frame(start = c(1, 51), end = c(20, 70)), "+", gen, "chr1")
}

}
