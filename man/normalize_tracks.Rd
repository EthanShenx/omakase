% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{normalize_tracks}
\alias{normalize_tracks}
\title{Normalise coverage tracks}
\usage{
normalize_tracks(
  x,
  method = "none",
  library_sizes = NULL,
  factors = NULL,
  bin_width = NULL,
  scale_junctions = TRUE
)
}
\arguments{
\item{x}{A \code{sashimi_data} object.}

\item{method}{One of \code{\link[=normalize_methods]{normalize_methods()}}.}

\item{library_sizes}{Named numeric vector of \eqn{N_j}, named by sample. If
absent for \code{cpm}/\code{rpm}/\code{rpkm}, the sum of the plotted signal is used
instead, and a note is emitted - a window-local total is not a library
size, and only makes panels comparable in a rough sense.}

\item{factors}{Named numeric vector of divisors for \code{method = "manual"}.}

\item{bin_width}{Bin width in base pairs, needed for \code{rpkm}. Taken from the
track spacing when \code{NULL}.}

\item{scale_junctions}{If \code{TRUE}, junction counts are divided by the same
per-sample factor as the coverage, so arc labels stay on the same scale as
the track beneath them.}
}
\value{
The \code{sashimi_data} object with rescaled values and a record of the
scaling in \code{meta}.
}
\description{
Rescales the \code{value} column of a \code{sashimi_data} object's \code{tracks} slot (and,
where it makes sense, the matching junction counts) so panels from libraries
of different depth are comparable.
}
\details{
With \eqn{c_i} the raw signal in bin \eqn{i} of sample \eqn{j}, \eqn{N_j}
the library size and \eqn{L} the bin width in kilobases:

\describe{
\item{\code{none}}{\eqn{c_i}}
\item{\code{cpm}, \code{rpm}}{\eqn{c_i \times 10^6 / N_j}}
\item{\code{rpkm}}{\eqn{c_i \times 10^9 / (N_j L)}}
\item{\code{size_factor}}{\eqn{c_i / s_j}, with DESeq2's median-of-ratios
factor \eqn{s_j = \mathrm{median}_i \left( k_{ij} / (\prod_v k_{iv})^{1/m} \right)}
over the \eqn{m} samples, taken across bins with non-zero signal in
every sample}
\item{\code{manual}}{\eqn{c_i / f_j} for user-supplied factors \code{factors}}
\item{\code{max}}{each sample scaled to a maximum of 1}
\item{\code{sum}}{each sample scaled to sum to 1}
}
}
\examples{
sd <- sashimi_data(
  loci = data.frame(locus_id = "a", gene_name = "A", chrom = "chr1",
                    strand = "+", win_lo = 1, win_hi = 100),
  tracks = data.frame(locus_id = "a", group = "g", sample = "s1",
                      pos = 1:100, value = 1)
)
normalize_tracks(sd, "cpm")$tracks$value[1]

}
