% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-tracks.R
\name{track_features}
\alias{track_features}
\title{Build a feature track}
\usage{
track_features(
  features,
  title = NULL,
  color = OM_FEATURE_FILL,
  labels = TRUE,
  shape = c("block", "marker"),
  collapse = TRUE,
  height = 0.4
)
}
\arguments{
\item{features}{A data frame with \code{start}, \code{end} and optionally \code{name}, a
\code{sashimi_data} object, or a path to a BED file.}

\item{title}{Track title.}

\item{color}{Fill; \code{"bed_rgb"} uses each record's own colour.}

\item{labels}{Print feature names.}

\item{shape}{\code{"block"} or \code{"marker"}.}

\item{collapse}{Draw every feature on one row rather than spreading
overlapping ones across rows.}

\item{height}{Relative height of this track.}
}
\value{
An \code{omakase_track} object.
}
\description{
A row of blocks - peaks, repeats, motifs, start sites - optionally labelled.
With \code{shape = "marker"} each feature is drawn as a small downward pointer
rather than a block, which is what a single-base position wants.
}
\examples{
f <- data.frame(start = c(100, 500), end = c(200, 560),
                name = c("peak1", "peak2"))
track_features(f, title = "peaks")
}
