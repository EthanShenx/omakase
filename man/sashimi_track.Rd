% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-sashimi.R
\name{sashimi_track}
\alias{sashimi_track}
\title{Draw a single coverage panel with its junction arcs}
\usage{
sashimi_track(
  x,
  locus = NULL,
  group = NULL,
  xlim = NULL,
  ymax = NULL,
  map = NULL,
  fill = "#66C2A5",
  arc_color = NULL,
  alpha = 1,
  base_size = 9,
  base_family = "",
  arc_shape = "sine",
  arc_height_rule = "auto",
  arc_height_frac = c(0.8, 1.2),
  arc_width_rule = "constant",
  arc_width = 0.5,
  arc_side = "above",
  arc_n = 121,
  show_arc_label = TRUE,
  arc_label_format = "activity",
  arc_label_position = c("on", "above", "none"),
  label_background = TRUE,
  label_padding = 0.6,
  label_offset = 0.03,
  background = NA,
  background_alpha = 1,
  min_count = 0,
  group_label = NULL,
  psi_label = NULL,
  psi_pad = 0.3,
  log_y = FALSE,
  overlay_junction_fun = "mean",
  theme = NULL
)
}
\arguments{
\item{x}{A \code{sashimi_data} object.}

\item{locus}{A \code{locus_id} or gene name. Defaults to the first locus.}

\item{group}{The group (sample, stage, condition) to draw. Defaults to the
first. Several groups may be given, in which case their coverage is
overlaid in this one panel, each in its own colour from \code{fill}.}

\item{xlim}{Plot-space x limits; defaults to the locus window widened by
\code{psi_pad}.}

\item{ymax}{Upper limit of the coverage axis. \code{NULL} takes the panel's own
maximum, which is what makes each panel use its full height; pass a shared
value (as \code{\link[=plot_sashimi]{plot_sashimi()}} does when \code{fix_y_scale = TRUE}) to make panels
quantitatively comparable.}

\item{map}{An \code{\link[=intron_map]{intron_map()}} for compressed coordinates, or \code{NULL}.}

\item{fill, arc_color}{Colours for the coverage area and the arcs. \code{arc_color}
defaults to \code{fill}.}

\item{alpha}{Opacity of the coverage area.}

\item{base_size}{Base font size in points.}

\item{base_family}{Font family for every piece of text in the figure. The
empty string uses the graphics device's default. Text geoms do not inherit
a theme's family, so this is threaded to each of them explicitly.}

\item{arc_shape}{One of \code{\link[=arc_shapes]{arc_shapes()}}.}

\item{arc_height_rule}{One of \code{\link[=arc_height_rules]{arc_height_rules()}}. \code{"auto"} (the default)
staggers a handful of arcs at the \code{arc_height_frac} heights and switches to
\code{"span"} once there are enough junctions that equal-height arcs would
cross; \code{"constant"} always staggers; \code{"span"} scales height with junction
width so nested junctions nest; the rest scale height with count.}

\item{arc_height_frac}{Arc apex heights as a fraction of \code{ymax}. The values
are cycled across arcs - arcs sharing an end point first - so their labels
cannot collide.}

\item{arc_width_rule, arc_width}{Junction line width: a rule from
\code{\link[=arc_widths]{arc_widths()}} and its scale factor.}

\item{arc_side}{\code{"above"} or \code{"below"} the coverage.}

\item{arc_n}{Points per arc path.}

\item{show_arc_label}{Print the count on each arc.}

\item{arc_label_format}{A formatter: a function, or one of \code{"activity"},
\code{"count"}, \code{"coord"}, \code{"none"}.}

\item{arc_label_position}{Where the count sits relative to its arc.
\code{"on"} (the default) puts it astride the apex, its opaque box
interrupting the curve, which keeps the digits readable where arcs cross;
\code{"above"} floats it clear of an unbroken curve; \code{"none"} is the same as
\code{show_arc_label = FALSE}.}

\item{label_background}{Draw the arc label on an opaque box, so an arc
passing underneath does not strike through the digits. Only applies when
\code{arc_label_position = "on"}.}

\item{label_padding}{Padding inside the label box, in points.}

\item{label_offset}{Gap between the arc apex and a label placed \code{"above"},
as a fraction of the panel height.}

\item{background}{Fill for the coverage panel. \code{NA} (the default) leaves it
transparent; a colour tints the whole panel, which separates stacked
tracks without drawing rules between them.}

\item{background_alpha}{Opacity of \code{background}.}

\item{min_count}{Junctions with a count below this are not drawn.}

\item{group_label}{Text printed at the top left of the panel. \code{NULL} uses
the group name; \code{NA} prints nothing.}

\item{psi_label}{Text printed in the right-hand gutter, or \code{NULL} to take it
from the \code{psi} slot.}

\item{psi_pad}{Width of the right-hand gutter, as a fraction of the window.}

\item{log_y}{Draw the coverage on a \code{log10(1 + value)} axis.}

\item{overlay_junction_fun}{How a junction shared by several overlaid groups
is combined into one arc: \code{"mean"} (the default), \code{"median"}, \code{"sum"} or
\code{"max"}. Only used when \code{group} names more than one group.}

\item{theme}{A ggplot2 theme, or \code{NULL} for \code{\link[=theme_omakase]{theme_omakase()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
One row of a sashimi figure. Exported so a panel can be built, inspected or
restyled on its own; \code{\link[=plot_sashimi]{plot_sashimi()}} calls this once per group and stacks
the results.
}
\examples{
sd <- sashimi_data(
  loci = data.frame(locus_id = "a", gene_name = "A", chrom = "chr1",
                    strand = "+", win_lo = 1000, win_hi = 2000),
  tracks = data.frame(locus_id = "a", group = "g", pos = seq(1000, 2000, 10),
                      value = abs(sin(seq(0, pi, length.out = 101))) * 20),
  junctions = data.frame(locus_id = "a", group = "g", x0 = 1200, x1 = 1800,
                         count = 42)
)
sashimi_track(sd)

}
