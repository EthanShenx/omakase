% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-rmats.R
\name{read_rmats}
\alias{read_rmats}
\title{Read an rMATS event table}
\usage{
read_rmats(
  path,
  event_type = NULL,
  fdr = NULL,
  min_dpsi = NULL,
  top = NULL,
  genes = NULL,
  chrom_style = "keep",
  flank = 300
)
}
\arguments{
\item{path}{Path to the rMATS output file.}

\item{event_type}{One of \code{\link[=event_types]{event_types()}}. Inferred from the file name when
\code{NULL}.}

\item{fdr}{Keep only events with \code{FDR} at or below this.}

\item{min_dpsi}{Keep only events whose absolute \code{IncLevelDifference} is at
least this.}

\item{top}{Keep only the \code{top} most significant events.}

\item{genes}{Restrict to these gene names or IDs.}

\item{chrom_style}{Rewrite contig names: \code{"keep"}, \code{"ucsc"} (ensure a \code{chr}
prefix) or \code{"ensembl"} (strip it), for when the event file and the BAM
header disagree.}

\item{flank}{Padding added around the event when computing its window.}
}
\value{
A data frame with one row per event: \code{event_id}, \code{event_type},
\code{gene_id}, \code{gene_name}, \code{chrom}, \code{strand}, the exon coordinates, the
window (\code{win_lo}, \code{win_hi}), and rMATS's \code{psi_1}, \code{psi_2}, \code{dpsi},
\code{pvalue}, \code{fdr}.
}
\description{
Reads a \verb{*.MATS.JC.txt} or \verb{*.MATS.JCEC.txt} file and returns one row per
event with the coordinates needed to draw it, plus rMATS's own inclusion
levels and significance.
}
\examples{
f <- system.file("extdata", "SE.MATS.JC.txt", package = "omakase")
if (nzchar(f)) read_rmats(f, "SE", fdr = 0.05, top = 20)

}
