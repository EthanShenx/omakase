% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-contract.R
\name{write_sashimi_data}
\alias{write_sashimi_data}
\title{Write a sashimi data object to disk}
\usage{
write_sashimi_data(x, dir, prefix = "sashimi", parquet = FALSE)
}
\arguments{
\item{x}{A \code{sashimi_data} object.}

\item{dir}{Output directory, created if absent.}

\item{prefix}{File name prefix.}

\item{parquet}{If \code{TRUE} and the arrow package is available, write the
(largest) \code{tracks} slot as parquet instead of TSV.}
}
\value{
The character vector of written paths, invisibly.
}
\description{
Writes one file per non-empty slot, plus a \verb{*_methods.tsv} recording \code{meta}.
The layout mirrors what \code{\link[=sashimi_from_tables]{sashimi_from_tables()}} reads back, so a slow
counting step can be run once and re-plotted cheaply.
}
\examples{
sd <- sashimi_data(loci = data.frame(locus_id = "a", gene_name = "A",
  chrom = "chr1", strand = "+", win_lo = 1, win_hi = 10))
write_sashimi_data(sd, tempdir(), prefix = "demo")
}
