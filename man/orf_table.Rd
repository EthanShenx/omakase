% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consequence-orf.R
\name{orf_table}
\alias{orf_table}
\title{Call ORFs for a set of transcript models}
\usage{
orf_table(models, genome, rule = "first", min_aa = 10, require_stop = TRUE)
}
\arguments{
\item{models}{A data frame with \code{tx_id}, \code{chrom}, \code{strand}, \code{start}, \code{end},
one row per exon.}

\item{genome}{A \code{BSgenome}, \code{DNAStringSet}, or path to an indexed FASTA.}

\item{rule, min_aa, require_stop}{Passed to \code{\link[=find_orf]{find_orf()}}.}
}
\value{
A data frame with one row per transcript: \code{tx_id}, \code{chrom},
\code{strand}, \code{tx_start}, \code{tx_end}, \code{first_exon_start}, \code{first_exon_end},
\code{tss_pos}, \code{protein}, \code{aa_len}, \code{utr5_len}, \code{n_uATG}.
}
\description{
Convenience wrapper that splices each transcript and calls its ORF, giving
the per-isoform table \code{\link[=classify_consequence]{classify_consequence()}} consumes.
}
\examples{
if (requireNamespace("Biostrings", quietly = TRUE)) {
  # A toy genome: a start codon, a short coding stretch, then a stop.
  genome <- Biostrings::DNAStringSet(c(
    chr1 = paste0(strrep("T", 60), "ATG", strrep("GCA", 30), "TAA",
                  strrep("T", 60))
  ))
  models <- data.frame(
    tx_id = "tx1", chrom = "chr1", strand = "+",
    start = c(1, 100), end = c(80, 214)
  )
  orf_table(models, genome)
}

}
