% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-consequence.R
\name{plot_consequence}
\alias{plot_consequence}
\title{Draw the consequence composition}
\usage{
plot_consequence(
  x,
  style = c("donut", "bar", "lollipop", "stacked"),
  by = c("subtype", "category"),
  filter = NULL,
  palette = NULL,
  title = NULL,
  base_size = 10,
  base_family = "",
  inner_r = 0.24,
  bar_range = c(0.11, 0.185),
  gap_deg = 2,
  start_deg = 90,
  show_percent = TRUE,
  show_n = FALSE,
  show_guides = TRUE,
  legend_position = "bottom"
)
}
\arguments{
\item{x}{A data frame from \code{\link[=classify_consequence]{classify_consequence()}} or
\code{\link[=consequence_summary]{consequence_summary()}}. A raw classified table is summarised
automatically.}

\item{style}{\code{"donut"}, \code{"bar"}, \code{"lollipop"} or \code{"stacked"}.}

\item{by}{\code{"subtype"} or \code{"category"}.}

\item{filter}{An optional expression evaluated in \code{x} to subset rows, for
example \code{both_full_length == 1}.}

\item{palette}{Named vector of category colours, or \code{NULL} for the default.}

\item{title}{Plot title, or \code{NULL}.}

\item{base_size}{Base font size in points.}

\item{base_family}{Font family for every piece of text in the figure. The
empty string uses the graphics device's default. Text geoms do not inherit
a theme's family, so this is threaded to each of them explicitly.}

\item{inner_r}{Inner radius of the ring, in the plot's arbitrary units.}

\item{bar_range}{Length-2 numeric giving the minimum and maximum bar height.}

\item{gap_deg}{Angular gap between sectors, in degrees.}

\item{start_deg}{Angle at which the first sector begins; 90 puts it at the
top.}

\item{show_percent}{Print each sector's share inside the ring.}

\item{show_n}{Append the raw count to each external label.}

\item{show_guides}{Draw the dotted magnitude guide circles.}

\item{legend_position}{Where to put the category legend.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualises how a set of start-site switches divides among consequence
classes. Four styles are available; the donut is the default and is the one
the package's house design was built around.
}
\section{Geometry of the donut}{

The \eqn{n} subtypes each occupy an equal angular sector of
\eqn{(360 - n g)/n} degrees, separated by a gap of \eqn{g}. A sector's inner
radius is fixed at \code{inner_r}; its outer radius is
\eqn{r_i = r_0 + h_i} with the bar height \eqn{h_i} scaled linearly from the
counts,
\deqn{h_i = h_{\min} + \frac{n_i - \min n}{\max n - \min n}(h_{\max} - h_{\min}).}
So the \emph{radius} encodes abundance while the angle is constant - which is the
point of the design, since equal angles keep every label legible regardless
of how rare its category is.
}

\examples{
d <- data.frame(
  category = c(rep("5'UTR change", 3), "Promoter swap", rep("N-terminal/CDS", 2)),
  subtype = c("longer", "shorter", "equal", "alt first exon",
              "N-term extension", "ORF loss"),
  n = c(48, 31, 12, 22, 15, 8)
)
plot_consequence(d)
plot_consequence(d, style = "lollipop")

}
