% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-tables.R
\name{read_manifest}
\alias{read_manifest}
\title{Read a sample manifest}
\usage{
read_manifest(path, group_col = NULL, label_col = NULL, base_dir = NULL)
}
\arguments{
\item{path}{Path to the manifest, or a character vector of file paths, or a
single alignment file.}

\item{group_col}{Column (name or 1-based index) that assigns samples to
groups. Defaults to column 3 when present, otherwise every sample is its
own group.}

\item{label_col}{Column carrying the label to print on each panel. Defaults
to the sample identifier.}

\item{base_dir}{Directory that relative paths in the manifest are resolved
against; defaults to the manifest's own directory.}
}
\value{
A data frame with columns \code{sample}, \code{path}, \code{group}, \code{label}, plus
any extra manifest columns.
}
\description{
Reads the tab-separated manifest used to describe a set of alignment files.
The format matches the one \code{ggsashimi} accepts, so an existing manifest
works unchanged: column 1 is a sample identifier, column 2 the path to the
file, and any further columns are metadata that can be named in \code{group_col}
or \code{color_col}.
}
\details{
A header row is optional. When absent, columns are named \code{sample}, \code{path},
\code{V3}, \code{V4} and so on.
}
\examples{
m <- tempfile(fileext = ".tsv")
writeLines(c("s1\ta.bam\tEndothelial", "s2\tb.bam\tEpithelial"), m)
read_manifest(m)

}
