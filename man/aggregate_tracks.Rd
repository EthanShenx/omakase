% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{aggregate_tracks}
\alias{aggregate_tracks}
\title{Aggregate replicate tracks into one track per group}
\usage{
aggregate_tracks(
  x,
  fun = c("mean", "median", "sum", "max", "none"),
  junction_fun = NULL
)
}
\arguments{
\item{x}{A \code{sashimi_data} object.}

\item{fun}{One of \code{"mean"}, \code{"median"}, \code{"sum"}, \code{"max"}, or \code{"none"} to
leave the object untouched.}

\item{junction_fun}{Aggregation applied to junction counts; defaults to the
same as \code{fun}. \code{"sum"} is the honest choice when arcs should reflect total
support across replicates.}
}
\value{
The aggregated \code{sashimi_data} object.
}
\description{
Replicates are usually plotted one panel each, but with many samples that is
unreadable. Aggregation collapses each group to a single track, the way
\code{ggsashimi}'s \code{--aggr} does, with the option to keep junction counts summed
while the coverage is averaged.
}
\examples{
sd <- sashimi_data(
  loci = data.frame(locus_id = "a", gene_name = "A", chrom = "chr1",
                    strand = "+", win_lo = 1, win_hi = 3),
  tracks = data.frame(locus_id = "a", group = "g",
                      sample = rep(c("s1", "s2"), each = 3),
                      pos = rep(1:3, 2), value = c(1, 2, 3, 3, 4, 5))
)
aggregate_tracks(sd, "mean")$tracks
}
