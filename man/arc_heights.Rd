% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arcs.R
\name{arc_heights}
\alias{arc_heights}
\title{Compute arc apex heights from junction counts}
\usage{
arc_heights(
  count,
  rule = "constant",
  min_h = 0,
  max_h = 1,
  base = 10,
  span = NULL
)
}
\arguments{
\item{count}{Numeric vector of junction counts or activities.}

\item{rule}{One of \code{\link[=arc_height_rules]{arc_height_rules()}}. \code{"constant"} gives every arc the
same height, which keeps the picture legible when counts span orders of
magnitude; \code{"span"} scales height with the width of the junction, so
nested junctions nest visually instead of crossing; the rest scale height
with count.}

\item{min_h, max_h}{The height range to map onto, in coverage-axis units.}

\item{base}{Logarithm base used by \code{rule = "log"}.}

\item{span}{Junction widths, required for \code{rule = "span"}.}
}
\value{
A numeric vector of heights, the same length as \code{count}.
}
\description{
Compute arc apex heights from junction counts
}
\examples{
arc_heights(c(1, 10, 100), rule = "log", min_h = 1, max_h = 5)

}
