% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-tables.R
\name{sashimi_from_tables}
\alias{sashimi_from_tables}
\title{Build a sashimi data object from tidy tables}
\usage{
sashimi_from_tables(
  loci = NULL,
  tracks = NULL,
  junctions = NULL,
  models = NULL,
  psi = NULL,
  features = NULL,
  rename = list(),
  locus_col = NULL,
  meta = list()
)
}
\arguments{
\item{loci, tracks, junctions, models, psi, features}{Data frames, or paths to
files. Any may be \code{NULL}.}

\item{rename}{A named list of named character vectors, one per slot, mapping
contract column names to the names used in your tables. For example
\code{rename = list(tracks = c(group = "stage", value = "rpm"))} reads a table
whose columns are \code{stage} and \code{rpm}.}

\item{locus_col}{The column identifying the locus, if it is not \code{locus_id}.
A common case is a gene name column shared by every table.}

\item{meta}{A named list recorded on the object.}
}
\value{
A \code{sashimi_data} object.
}
\description{
Accepts data frames or paths to TSV/CSV/parquet files, one per slot, and
maps their columns onto the \code{\link[=sashimi_data]{sashimi_data()}} contract. Column names that
differ from the contract can be remapped through \code{rename}, so tables written
by an existing pipeline usually need no editing.
}
\examples{
loci <- data.frame(gene_name = "DEMO", chrom = "chr1", strand = "+",
                   win_lo = 1000, win_hi = 2000)
tracks <- data.frame(gene_name = "DEMO", stage = "early",
                     pos = seq(1000, 2000, 100), rpm = runif(11))
sashimi_from_tables(
  loci = loci, tracks = tracks,
  rename = list(tracks = c(group = "stage", value = "rpm")),
  locus_col = "gene_name"
)

}
