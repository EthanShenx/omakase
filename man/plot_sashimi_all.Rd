% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-sashimi.R
\name{plot_sashimi_all}
\alias{plot_sashimi_all}
\title{Draw a sashimi figure for every locus}
\usage{
plot_sashimi_all(
  x,
  dir = NULL,
  device = "pdf",
  width = 5.9,
  height = NULL,
  dpi = 300,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{sashimi_data} object.}

\item{dir}{Directory to write into, or \code{NULL} to return the plots without
writing.}

\item{device}{File extension: \code{"pdf"}, \code{"png"}, \code{"svg"}, \code{"tiff"}, \code{"jpeg"}.}

\item{width}{Figure width in inches.}

\item{height}{Figure height in inches, or \code{NULL} to size it from the number
of panels.}

\item{dpi}{Resolution for raster devices.}

\item{quiet}{Suppress the per-file progress message.}

\item{...}{Passed to \code{\link[=plot_sashimi]{plot_sashimi()}}.}
}
\value{
A named list of plot objects, invisibly when writing to disk.
}
\description{
Loops \code{\link[=plot_sashimi]{plot_sashimi()}} over the loci in an object, optionally writing one
file per locus.
}
\examples{
sd <- sashimi_data(
  loci = data.frame(locus_id = c("a", "b"), gene_name = c("A", "B"),
                    chrom = "chr1", strand = "+",
                    win_lo = 1, win_hi = 100),
  tracks = data.frame(locus_id = rep(c("a", "b"), each = 10), group = "g",
                      pos = rep(seq(1, 100, length.out = 10), 2),
                      value = runif(20))
)
plots <- plot_sashimi_all(sd)
names(plots)
}
