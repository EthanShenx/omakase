% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compress.R
\name{intron_map}
\alias{intron_map}
\title{Build a genome-to-plot coordinate map that compresses introns}
\usage{
intron_map(
  introns,
  win_lo,
  win_hi,
  method = "power",
  gamma = 0.7,
  cap = 200,
  scale = 5,
  coef = 20,
  min_intron = 100,
  exon_scale = 1
)
}
\arguments{
\item{introns}{A two-column data frame or matrix of intron \code{start}/\code{end}
coordinates, or \code{NULL} for no compression. Overlapping intervals are
merged; intervals are sorted internally.}

\item{win_lo, win_hi}{The plotted window. Introns are clipped to it.}

\item{method}{One of \code{\link[=shrink_methods]{shrink_methods()}}.}

\item{gamma}{Exponent for \code{method = "power"}.}

\item{cap}{Maximum drawn intron length for \code{method = "fixed"}.}

\item{scale}{Divisor for \code{method = "scale"}.}

\item{coef}{Multiplier \eqn{c} for \code{method = "log"}.}

\item{min_intron}{Introns shorter than this are left uncompressed; shrinking
a 60 bp intron buys no width and distorts short-intron genes.}

\item{exon_scale}{Divisor applied to exonic stretches, the counterpart of
\code{rmats2sashimiplot}'s \code{--exon_s}. The default of \code{1} leaves exons at true
scale; larger values shrink them too, which is occasionally wanted when a
single huge exon dominates the view.}
}
\value{
An object of class \code{omakase_intron_map} with elements \code{genome} and
\code{plot} (the knot coordinates), plus the settings used. Apply it with
\code{\link[=compress_coords]{compress_coords()}} and invert it with \code{\link[=expand_coords]{expand_coords()}}.
}
\description{
Given a window and a set of intronic intervals, constructs a monotone
piecewise-linear map \eqn{\phi} that leaves exonic sequence at scale 1 and
draws each intron of length \eqn{L = b - a} at a reduced length \eqn{g(L)}:
}
\details{
\describe{
\item{\code{none}}{\eqn{g(L) = L}}
\item{\code{power}}{\eqn{g(L) = L^{\gamma}}, default \eqn{\gamma = 0.7}
(the \code{ggsashimi} rule)}
\item{\code{log}}{\eqn{g(L) = c \log(1 + L)}}
\item{\code{fixed}}{\eqn{g(L) = \min(L, k)}}
\item{\code{scale}}{\eqn{g(L) = L / s\,} (the \code{rmats2sashimiplot --intron_s}
rule)}
}

Writing the cumulative shift after the \eqn{j}th intron as
\eqn{\Delta_j = \sum_{i \le j} (L_i - g(L_i))}, the map is
\eqn{\phi(x) = x - \Delta_{j(x)}} on exonic stretches, and interpolates
linearly across each compressed intron. Because \eqn{\phi} is strictly
increasing it has an exact inverse, which is what \code{\link[=plot_sashimi]{plot_sashimi()}} uses to
label the axis with true genomic coordinates.
}
\examples{
m <- intron_map(data.frame(start = c(120, 400), end = c(300, 900)),
                win_lo = 100, win_hi = 1000)
compress_coords(c(100, 300, 1000), m)
# phi is exactly invertible
expand_coords(compress_coords(c(150, 950), m), m)

}
