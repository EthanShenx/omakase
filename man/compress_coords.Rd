% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compress.R
\name{compress_coords}
\alias{compress_coords}
\alias{expand_coords}
\title{Apply or invert an intron compression map}
\usage{
compress_coords(x, map)

expand_coords(x, map)
}
\arguments{
\item{x}{Numeric vector of coordinates.}

\item{map}{An \code{omakase_intron_map} from \code{\link[=intron_map]{intron_map()}}.}
}
\value{
A numeric vector the same length as \code{x}.
}
\description{
\code{compress_coords()} is \eqn{\phi}: genome coordinates to plot coordinates.
\code{expand_coords()} is \eqn{\phi^{-1}}. Values outside the mapped window are
extrapolated at scale 1, so an arc anchored just beyond the window still
lands somewhere sensible.
}
\examples{
m <- intron_map(data.frame(start = 200, end = 800), 100, 1000)
compress_coords(c(100, 500, 1000), m)
expand_coords(compress_coords(500, m), m)
}
