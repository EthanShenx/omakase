% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psi.R
\name{compute_psi}
\alias{compute_psi}
\title{Compute PSI (percent spliced in) per locus and group}
\usage{
compute_psi(
  x,
  main = "main",
  alt = c("alt", "ATSS", "alternative"),
  method = c("ratio", "rmats"),
  len_inc = NULL,
  len_skip = NULL
)
}
\arguments{
\item{x}{A \code{sashimi_data} object whose \code{junctions} slot carries a \code{role}
column, or a data frame with columns \code{locus_id}, \code{group}, \code{role} and
\code{count}.}

\item{main, alt}{The \code{role} values treated as the reference and alternative
features.}

\item{method}{\code{"ratio"} (the default) or \code{"rmats"}.}

\item{len_inc, len_skip}{Effective lengths \eqn{\ell_I}, \eqn{\ell_S} used by
\code{method = "rmats"}.}
}
\value{
For a \code{sashimi_data} input, the object with its \code{psi} slot filled
in. For a data frame, a data frame of \code{locus_id}, \code{group}, \code{psi},
\code{numerator}, \code{denominator}.
}
\description{
Compute PSI (percent spliced in) per locus and group
}
\details{
The \code{"ratio"} method is the share of activity carried by the reference
feature,
\deqn{\psi = \frac{A_{\mathrm{main}}}{A_{\mathrm{main}} + A_{\mathrm{alt}}},}
which is well defined for TSS activities, junction counts, or anything else
additive. Where both terms are zero, \eqn{\psi} is \code{NA} rather than 0 - no
signal is not the same as no inclusion.

The \code{"rmats"} method is the length-corrected inclusion level
\deqn{\psi = \frac{I / \ell_I}{I / \ell_I + S / \ell_S},}
where \eqn{I} and \eqn{S} are inclusion and skipping counts and
\eqn{\ell_I}, \eqn{\ell_S} the effective lengths, defaulting to the values
rMATS itself uses when they are not supplied.
}
\examples{
j <- data.frame(
  locus_id = "a", group = c("early", "early", "late", "late"),
  role = c("main", "alt", "main", "alt"), count = c(90, 10, 20, 80)
)
compute_psi(j)

}
