% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-annotation.R
\name{sashimi_annotation}
\alias{sashimi_annotation}
\title{Draw the annotation panel for one locus}
\usage{
sashimi_annotation(
  x,
  locus = NULL,
  xlim = NULL,
  map = NULL,
  base_size = 9,
  base_family = "",
  hairline = 0.3,
  role_fill = NULL,
  feature_color = OM_FEATURE_FILL,
  chrom_style = c("keep", "ucsc", "ensembl"),
  show_coord_bar = TRUE,
  coord_ticks = TRUE,
  show_tx_label = FALSE,
  show_features = TRUE,
  show_apex = TRUE,
  show_gene_label = TRUE,
  collapse = FALSE,
  arrow_bins = 0,
  theme = NULL
)
}
\arguments{
\item{x}{A \code{sashimi_data} object.}

\item{locus}{A \code{locus_id} or gene name. Defaults to the first locus.}

\item{xlim}{Plot-space x limits. Defaults to the locus window, widened by
\code{psi_pad}.}

\item{map}{An \code{\link[=intron_map]{intron_map()}} to draw in compressed coordinates, or \code{NULL}.}

\item{base_size}{Base font size in points.}

\item{base_family}{Font family for every piece of text in the figure. The
empty string uses the graphics device's default. Text geoms do not inherit
a theme's family, so this is threaded to each of them explicitly.}

\item{hairline}{Line width for the coordinate bar and intron lines.}

\item{role_fill}{Named vector of fills keyed by model \code{role}.}

\item{feature_color}{Fill for the feature boxes.}

\item{chrom_style}{How the contig name is printed above the coordinate bar:
\code{"keep"} (the default) prints it exactly as the data has it, \code{"ucsc"}
ensures a \code{chr} prefix, \code{"ensembl"} strips one.}

\item{show_coord_bar}{Draw the coordinate bar and window end labels.}

\item{coord_ticks}{Intermediate tick marks on the coordinate bar, labelled
with the genomic coordinate each plot position maps back to. \code{TRUE} (the
default) draws them only when introns are compressed, where an axis with
just two labelled ends would imply an even scale that is not there. Pass a
number for that many ticks, or \code{FALSE} for none.}

\item{show_tx_label}{Print each transcript's identifier at the left of its
row.}

\item{show_features}{Draw the \code{features} slot.}

\item{show_apex}{Mark \code{main_apex}/\code{alt_apex} from the \code{loci} slot with a
downward triangle.}

\item{show_gene_label}{Print the gene name, in italic, under the models.}

\item{collapse}{Draw one merged row per \code{role} instead of one row per
transcript. Useful when an annotation has thirty isoforms and only the
reference/alternative distinction matters.}

\item{arrow_bins}{Number of strand arrowheads to place along each intron
line. \code{0} disables them.}

\item{theme}{A ggplot2 theme, or \code{NULL} for \code{\link[=theme_omakase]{theme_omakase()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Returns the bottom panel of a sashimi figure as a standalone \code{ggplot}: the
coordinate bar, the transcript models, and any features. Called by
\code{\link[=plot_sashimi]{plot_sashimi()}}, but exported so the panel can be used on its own or
restyled before being assembled.
}
\examples{
sd <- sashimi_data(
  loci = data.frame(locus_id = "a", gene_name = "DEMO", chrom = "chr1",
                    strand = "+", win_lo = 1000, win_hi = 5000),
  models = data.frame(locus_id = "a", tx_id = "tx1", role = "main",
                      start = c(1200, 3000), end = c(1600, 4200))
)
sashimi_annotation(sd)

}
