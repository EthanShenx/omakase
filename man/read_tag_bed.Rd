% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-tags.R
\name{read_tag_bed}
\alias{read_tag_bed}
\title{Read a 5'-tag BED file}
\usage{
read_tag_bed(path, region = NULL, strand = NULL, count_col = 5)
}
\arguments{
\item{path}{Path to the BED file, optionally gzipped.}

\item{region}{Optional region to restrict to.}

\item{strand}{Keep only tags on this strand (\code{"+"}, \code{"-"}, or \code{NULL} for
both).}

\item{count_col}{Column holding a per-tag count. \code{NULL} treats every row as
one tag.}
}
\value{
A data frame with \code{chrom}, \code{pos}, \code{count}, \code{strand}.
}
\description{
Reads single-base transcription start site tags. Any BED-like file works:
columns 1-3 are contig, start, end, column 5 is taken as a count if present,
and column 6 as strand.
}
\examples{
f <- tempfile()
writeLines(c("chr1\t999\t1000\ttag1\t3\t+"), f)
read_tag_bed(f)

}
