% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_omakase_axes}
\alias{theme_omakase_axes}
\title{A themed variant that keeps the axes}
\usage{
theme_omakase_axes(
  base_size = 9,
  base_family = "",
  hairline = 0.3,
  margin = c(1, 4, 1, 4),
  legend = "none",
  grid = FALSE
)
}
\arguments{
\item{base_size}{Base font size in points. Everything else is expressed
relative to it.}

\item{base_family}{Font family. The empty string uses the device default.}

\item{hairline}{Line width for the axis rules.}

\item{margin}{Plot margin in points, given as a length-4 numeric in the
order top, right, bottom, left.}

\item{legend}{Legend position; \code{"none"} by default.}

\item{grid}{Whether to draw a light horizontal grid.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object.
}
\description{
Same type and weight as \code{\link[=theme_omakase]{theme_omakase()}}, but with a visible x axis and y
axis. Use it when a panel is being read quantitatively - a coverage track
whose depth matters, or a standalone plot outside a sashimi stack.
}
\examples{
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) + geom_point() + theme_omakase_axes()
}
