% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-tables.R
\name{read_sashimi_dir}
\alias{read_sashimi_dir}
\title{Read a directory of sashimi tables}
\usage{
read_sashimi_dir(dir, prefix = NULL)
}
\arguments{
\item{dir}{Directory to read.}

\item{prefix}{Optional file name prefix to require.}
}
\value{
A \code{sashimi_data} object.
}
\description{
The counterpart to \code{\link[=write_sashimi_data]{write_sashimi_data()}}: reads back the per-slot files it
wrote. Files are matched by the slot name appearing in the file name, so
both \code{sashimi_tracks.tsv} and a prefixed \code{mystudy_tracks.parquet} are found.
}
\examples{
d <- tempfile(); dir.create(d)
sd <- sashimi_data(loci = data.frame(locus_id = "a", gene_name = "A",
  chrom = "chr1", strand = "+", win_lo = 1, win_hi = 10))
write_sashimi_data(sd, d)
read_sashimi_dir(d)
}
