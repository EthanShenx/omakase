% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-junctions.R
\name{read_junctions}
\alias{read_junctions}
\title{Read a splice junction file}
\usage{
read_junctions(
  path,
  format = c("auto", "star", "bed", "plain"),
  region = NULL,
  min_count = 1,
  include_multimappers = FALSE,
  annotated_only = FALSE
)
}
\arguments{
\item{path}{Path to the junction file.}

\item{format}{\code{"auto"}, \code{"star"}, \code{"bed"}, or \code{"plain"}.}

\item{region}{Optional region to restrict to.}

\item{min_count}{Drop junctions with fewer supporting reads.}

\item{include_multimappers}{For STAR input, add the multi-mapping read count
to the unique count.}

\item{annotated_only}{For STAR input, keep only junctions the annotation
already contains.}
}
\value{
A data frame with \code{chrom}, \code{x0}, \code{x1}, \code{count}, \code{strand}.
}
\description{
Reads STAR \code{SJ.out.tab}, a regtools/TopHat junction BED, or a plain BED-like
table of junctions. The format is detected from the columns.
}
\details{
STAR's \code{SJ.out.tab} has no header and nine columns: contig, first intron
base, last intron base, strand code (\code{0} undefined, \code{1} \code{+}, \code{2} \code{-}),
intron motif, annotated flag, uniquely-mapping reads, multi-mapping reads,
maximum overhang. The uniquely-mapping count is used unless
\code{include_multimappers} is set.

A regtools junction BED has twelve columns, where the thick start/end and
block sizes describe the anchors flanking the intron; the intron itself is
recovered from them.
}
\examples{
f <- tempfile()
writeLines("chr1\t100\t200\t1\t1\t1\t42\t3\t30", f)
read_junctions(f)

}
